<UserControl x:Class="WindowsModern.FeedTile.TilePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsModern.FeedTile"
             mc:Ignorable="d" Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded" SizeChanged="UserControl_SizeChanged"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="7,0,7,0" Height="Auto">
		<Grid>
			<Grid>
				<WrapPanel Name="LoadingPanel" Orientation="Horizontal" Margin="3,10,3,10" VerticalAlignment="Center" HorizontalAlignment="Center">
					<Image Name="LoadingImage" Width="16"></Image>
					<TextBlock Margin="10,0,0,0" Name="LoadingText" Style="{DynamicResource GlobalTextBlockTextDisplay}">Loading...</TextBlock>
				</WrapPanel>
			</Grid>
			<Grid>
				<WrapPanel Name="NoItemsPanel" Orientation="Horizontal" Margin="3,10,3,10" VerticalAlignment="Center" HorizontalAlignment="Center">
					<TextBlock Name="NoItemsText" Style="{DynamicResource GlobalTextBlockTextDisplay}">There is no items.</TextBlock>
				</WrapPanel>
			</Grid>
			<StackPanel Orientation="Vertical" Name="FeedItemsContainerBehind" VerticalAlignment="Top">

			</StackPanel>
			<StackPanel Orientation="Vertical" Name="FeedItemsContainerFront" VerticalAlignment="Top">

			</StackPanel>
		</Grid>
    </Grid>
</UserControl>
